import {listContentLibrary} from '../../lib/db';
export default function PendingPublishPanel(){const items:any[]=listContentLibrary(300),pending=items.filter((x:any)=>x.statusLabel!=='已发布'&&(['visual','publish'].includes(x.currentStep)||x.statusLabel==='发布失败'));return <>{pending.length===0?<div className="card"><h2>暂时没有待发布内容</h2><p className="muted">可以先去选题开始一篇新内容。</p><a className="button-link" href="/topics">开始选题 →</a></div>:<div className="grid">{pending.map((x:any)=><section className="card span-12" key={x.id}><div className="meta"><span className="pill">{x.statusLabel}</span>{x.publishOutcome&&<span className="pill">{x.publishOutcome}</span>}</div><h2 style={{fontSize:22}}>{x.title}</h2><p className="muted">下一步已经按当前进度准备好，无需重新寻找入口。</p><a className="button-link" href={x.nextAction?.href||`/editor?id=${encodeURIComponent(x.id)}`}>{x.nextAction?.label||'继续处理'} →</a></section>)}</div>}</>}
