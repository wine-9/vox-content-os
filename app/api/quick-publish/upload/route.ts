import fs from'node:fs';import{Readable}from'node:stream';import{pipeline}from'node:stream/promises';import{directUploadPath,markDirectFileReady}from'../../../../lib/direct-publish';export const runtime='nodejs';export const maxDuration=600;
export async function PUT(req:Request){try{const u=new URL(req.url),job=u.searchParams.get('job')||'',file=u.searchParams.get('file')||'';if(!req.body)throw Error('上传内容为空');const fp=directUploadPath(job,file);await pipeline(Readable.fromWeb(req.body as any),fs.createWriteStream(fp));return Response.json({ok:true,job:markDirectFileReady(job,file)})}catch(e:any){return Response.json({ok:false,error:String(e.message||e)},{status:400})}}
