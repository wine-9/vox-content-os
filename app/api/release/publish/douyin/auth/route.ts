import{randomUUID}from'node:crypto';import{saveOauthState}from'../../../../../../lib/db';export const runtime='nodejs';export async function GET(req:Request){const u=new URL(req.url),contentId=u.searchParams.get('id')||'',key=process.env.DOUYIN_CLIENT_KEY,redirect=process.env.DOUYIN_REDIRECT_URI;if(!contentId||!key||!redirect)return Response.json({ok:false,error:'抖音 OAuth 尚未配置 DOUYIN_CLIENT_KEY / DOUYIN_REDIRECT_URI'},{status:409});const state=randomUUID();saveOauthState('douyin',contentId,state,Date.now()+10*60*1000);const a=new URL('https://open.douyin.com/platform/oauth/connect/');a.searchParams.set('client_key',key);a.searchParams.set('response_type','code');a.searchParams.set('scope','video.create.bind');a.searchParams.set('redirect_uri',redirect);a.searchParams.set('state',state);return Response.redirect(a.toString(),302)}
