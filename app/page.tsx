import SystemStatus from './SystemStatus';
import {dashboardStats} from '../lib/db';
export const dynamic='force-dynamic';
export default function Dashboard(){const s=dashboardStats();return <><div style={{display:'flex',justifyContent:'space-between',gap:16,alignItems:'end',marginBottom:20}}><div><h1>今日工作台</h1><div className="muted">目标：先把文稿做对，再锁定公众号视觉母版，最后适配各平台并进入发布准备。</div></div><span className="badge">dry_run_only · 自动实发关闭</span></div><div className="grid"><div className="card span-3"><div className="muted">可选选题</div><div className="kpi">{s.proposed}</div></div><div className="card span-3"><div className="muted">待给 Brief</div><div className="kpi">{s.awaiting}</div></div><div className="card span-3"><div className="muted">待盲选</div><div className="kpi">{s.blindReview}</div><div className="muted">生成中 {s.generatingCandidates}</div></div><div className="card span-3"><div className="muted">Winner 待精修</div><div className="kpi">{s.editing}</div></div><div className="card span-6"><h2>新的写作闭环</h2><p>真实热点 → Content Brief → Control / Human Writing / Ultimate Fusion 三稿 → 随机 A/B/C → 盲选 Winner → 人工 Final → Winner→Final Diff。</p><p className="muted">Final {s.finalApproved} · 待审风格 Proposal {s.pendingProposals}</p></div><div className="card span-6"><h2>发布链路</h2><p>Final → 公众号文案 → HTML + 图片 + 封面视觉母版 → 确认母版 → 小红书 / 抖音按母版重新排版 → 双端确认 → Ready to Publish。</p><p className="muted">母版待确认 {s.wechatCopyApproved} · 母版已锁定 {s.masterApproved} · 平台适配中 {s.platformAdapting} · 待发布 {s.readyToPublish}</p></div></div><SystemStatus/></>}
